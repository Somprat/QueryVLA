# Reconstructed training history

Scope: 69 commits reachable from all locally available Git refs, beginning July 10, 2026 and ending September 23, 2026; current repository logs and reports. This checkout is not shallow. This is a partial reconstruction, not a complete execution audit. Remote-only branches, deleted/unreachable commits, RunPod shell history, tmux scrollback, W&B, and external checkpoint directories were not accessed. Commit dates establish when evidence entered Git, not exact execution times.

## What actually has execution evidence

| Date / evidence date | Experiment | Evidence and interpretation |
|---|---|---|
| August 8 report | RGB vs RGB + spatial offline probes; seeds 41, 42, 43; 20 epochs each | Six completed probe runs reported, including test metrics. This is probe training, not action-policy fine-tuning. Exact shell invocations are unavailable. |
| August 20 commit f4291a3 | `codex_maniskill_startup_test_2--image_aug` | Saved config: full mode, frozen VLM, trainable action model, one GPU, batch 1, target one step. Config does not establish completion. |
| August 20 commit f4291a3 | `codex_maniskill_startup_test_3--image_aug` | Same recorded training settings. Config does not establish completion. |
| August 20 commit f4291a3 | `codex_local_llama_smoke--image_aug` | Same recorded training settings. Config does not establish completion. |

The recovered [August 20 report](recovered/f4291a3_TRAINING_PROGRESS.md) says startup reached model restoration and dataset normalization-statistics iteration, but the statistics pass was intentionally stopped before the first optimizer step. A restart was stopped during imports. It recommends a future one-step smoke test and then a 100-step job; neither recommendation proves those jobs completed. It does not map every described attempt to a run ID.

The [August 8 report](../../myMemoryVLA/reports/progress_2026-08-08.md) explicitly says a proposed 5,000-step spatial-only policy pilot was **not launched**. Its six probe artifacts were stored at `/workspace/multimodal/models/model_b/eval_simpler/probe_ablation_96/`, outside the versioned evidence available here.

Reports named `bridge_5k` document rollout/failure collection. Their names and a tmux session named `train` do not establish the training command or prove 5,000 optimizer updates completed.

## Commands and script history

- [Commands copied from historical documentation](documented_commands.md): five distinct blocks, including the original seven dataset launch commands, proposed ManiSkill smoke test, 10,000-step example, dry run, and Bridge resume example.
- [Every distinct historical shell launcher version](launcher_history.md): 28 path/content versions with original contents and source revisions. Shell launchers include Bridge, Fractal, LIBERO, real-world, ManiSkill, mixture training, and failure-fusion training. These are available configurations, not run records.
- [Machine-readable documented commands](documented_commands.json).

Additional Python training entry points exist in `myMemoryVLA/script/freeze_vlm_exp/train_spatial_probe.py`, `myMemoryVLA/script/freeze_vlm_exp/train_freeze_vlm.py`, and `myMemoryVLA/script/train/train_query_router.py`. Their existence does not establish execution; the launcher-version archive covers shell scripts, not every Python source revision. Vendor training documentation is excluded.

## Approximate commands reconstructed from the three saved configurations

These recreate the main recorded settings, not the original shell text or full environment. Run from `myMemoryVLA/` using the historical revision `f4291a3` for matching defaults. CUDA device selection was not recorded in the configs. The suffix `--image_aug` is generated by training code, so the base run IDs below omit it.

```bash
# Substitute one of these three base IDs:
# codex_maniskill_startup_test_2
# codex_maniskill_startup_test_3
# codex_local_llama_smoke
DATA_ROOT_DIR=/workspace/datasets \
PRETRAINED_CKPT=/workspace/multimodal/models/model_b/checkpoints/memvla-bridge.pt \
EXPERIMENT_MODE=full \
FREEZE_VLM=true \
FREEZE_ACTION_MODEL=false \
N_GPU=1 BATCH_SIZE=1 MAX_STEPS=1 SAVE_INTERVAL=9999 \
LEARNING_RATE=2e-5 IMAGE_AUG=true SHUFFLE_BUFFER_SIZE=4096 \
RUN_ID=codex_maniskill_startup_test_2 \
bash script/train/maniskill/train_maniskill.sh
```

Original saved configs: `myMemoryVLA/log/maniskill/<run-id>/config.json`. Each records the target step count; none contains a completed-step count or loss history.

## The supplied 6,500-step baseline command

No `6500` match was found in the searched shell-script, Markdown, or config.json Git history. The command supplied in this conversation is evidence of an intended configuration, not evidence that it executed. The current ManiSkill launcher validates booleans as lowercase `true` or `false`; `True` / `False` would be rejected. The checkpoint path must also be replaced with a real checkpoint.

## What would close the gaps

The most useful next sources are the training machine's shell history, saved terminal or tmux output, W&B run configs and metrics, and checkpoint folders containing config.json plus step-labelled checkpoints and optimizer state. Match run IDs to code revisions, actual completed steps, checkpoint lineage, and evaluation metrics. A shell history entry proves a command was entered, not that it finished.
